apiVersion: v1
kind: ServiceAccount
metadata:
  name: support-ops-agent
  namespace: support-ops-agent
  labels:
    app.kubernetes.io/name: support-ops-agent
  annotations:
    # Replace with your IAM role ARN when using IRSA for Secrets Manager + CloudWatch.
    eks.amazonaws.com/role-arn: arn:aws:iam::074388197187:role/support-ops-agent-irsa-role
